package team.backend.goWithMe.domain.info.enums;

public enum DistanceStep {
    ONE(1), TWO(2), THREE(3), FOUR(4);

    private int step;

    DistanceStep(int step){
        this.step = step;
    }

}
